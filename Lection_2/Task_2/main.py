print(set(input("Enter a sequence of elements separated by commas: ").split(", ")))
# First of all, we takes user's string and split it 
# After that we convert it to set to get unique items and display on the screen